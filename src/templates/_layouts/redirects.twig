{% extends '_layouts/cp' %}

{% set fullPageForm = true %}

{% set selectedSubnavItem = 'redirects' %}

{% set crumbs = [
    { label: craft.xsearch.name, url: url('xsearch') },
] %}

{% if pageTitle ?? false %}
    {% set docTitle = pageTitle ~ ' - ' ~ 'Redirects'|t('xsearch') ~ ' - ' ~ 'Search'|t('xsearch') %}
{% else %}
    {% set docTitle = 'Redirects'|t('xsearch') ~ ' - ' ~ 'Search'|t('xsearch') %}
{% endif %}
