{% extends '_layouts/cp' %}

{% set fullPageForm = true %}

{% set selectedSubnavItem = 'redirects' %}

{% set crumbs = [
    { label: craft.search.name, url: url('search') },
] %}

{% if pageTitle ?? false %}
    {% set docTitle = pageTitle ~ ' - ' ~ 'Redirects'|t('search') ~ ' - ' ~ 'Search'|t('search') %}
{% else %}
    {% set docTitle = 'Redirects'|t('search') ~ ' - ' ~ 'Search'|t('search') %}
{% endif %}
