{% extends '_layouts/cp' %}

{% set fullPageForm = true %}

{% set selectedSubnavItem = 'queries' %}

{% set crumbs = [
    { label: craft.xsearch.name, url: url('xsearch') },
] %}

{% if pageTitle ?? false %}
    {% set docTitle = pageTitle ~ ' - ' ~ 'Queries'|t('xsearch') ~ ' - ' ~ 'Search'|t('xsearch') %}
{% else %}
    {% set docTitle = 'Queries'|t('xsearch') ~ ' - ' ~ 'Search'|t('xsearch') %}
{% endif %}
